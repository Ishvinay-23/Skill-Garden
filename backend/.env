# Sample .env for Skill Garden backend
MONGO_URI=mongodb://localhost:27017/skillgarden
# Optionally set a separate test URI; if omitted, tests use in-memory MongoDB
MONGO_URI_TEST=
JWT_SECRET=your_jwt_secret_here
PORT=4000
NODE_ENV=development
